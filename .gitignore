# Python
__pycache__/
*.pyc
*.pyo

# Build outputs
/build/
build_*/
build_macos/
build_py314/
build_nuitka/
build_onefile/
build_release_v530/
_build_release_v530/
dist/
*.manifest
*.log
*.so
*.dylib
*.pyd

# Large runtime binaries (~100 MB each) and locally compiled binaries
runtime/mpv-2.dll
runtime/avcodec-62.dll
runtime/avfilter-11.dll
runtime/libedge264.*
runtime/mvc_demuxer_cpp*
edge264/libedge264.*
mvc_demuxer_cpp*.so
mvc_demuxer_cpp*.pyd

# Runtime / sensitive / transient
sylc_license.json
crash_log.txt
exe.out
exe.err

# Pre-optimization binaries and benchmark baselines (local rollback only)
.perf_backup/

# Editor / OS / venv
.idea/
.vscode/
.DS_Store
Thumbs.db
.venv/

# Optional non-commercial MatAnyone 2 developer runtime. The integration and
# notices are tracked; third-party source, weights and the 3+ GiB uv venv are not.
models_dev_nc/matanyone2_runtime/
models_dev_nc/matanyone2/*.pth
tools_dev/_vendor/MatAnyone2/

# Depth model weights & engine caches
models/*.onnx
models/*.engine
models/*.bin
ort_tensorrt/
